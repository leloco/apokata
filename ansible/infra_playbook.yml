---

- name: Setup dns servers
  hosts: dns_group
  become: true
  roles:
    - { role: common, tags: ['common'] }
    - { role: pihole_records, tags: ['pihole', 'pihole_records'] }
    - { role: pihole, tags: ['pihole'] }
    - { role: keepalived_pihole, tags: ['pihole', 'keepalived'] }

- name: Setup lxc "tang"
  hosts: tang
  become: true
  roles:
    - { role: common, tags: ['common'] }
    - { role: update, tags: ['update'] }
    - { role: tang, tags: ['tang'] }
