---
- name: Setup lxc "runner-alpha"
  hosts: runner_group
  become: yes
  roles:
    - { role: update, tags: ['update'] }
    - { role: docker, tags: ['docker'] }
    - { role: github_runner, tags: ['runner'] }
