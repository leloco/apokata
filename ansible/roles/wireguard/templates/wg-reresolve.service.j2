# Managed by Ansible

[Unit]
Description=WireGuard DNS Reresolve Service for {{ vault_interface }}
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/reresolve-dns.sh {{ vault_interface }}
