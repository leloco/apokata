name: "Rocket.Chat Notification"
description: "Send status notification to Rocket.Chat incoming webhook"
inputs:
  webhook-url:
    description: "Rocket.Chat Webhook URL"
    required: true
  job-status:
    description: "Status of the calling job"
    required: true
  branch:
    description: "Git Branch"
    required: false
    default: ""
  commit-msg:
    description: "Commit Message"
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Send Payload
      shell: bash
      env:
        ROCKETCHAT_WEBHOOK_URL: ${{ inputs.webhook-url }}
        JOB_STATUS: ${{ inputs.job-status }}
        INPUT_BRANCH: ${{ inputs.branch }}
        INPUT_COMMIT_MSG: ${{ inputs.commit-msg }}
        WORKFLOW_NAME: ${{ github.workflow }}
        HEAD_REF: ${{ github.head_ref }}
        REF_NAME: ${{ github.ref_name }}
        EVENT_NAME: ${{ github.event_name }}
        EVENT_COMMIT_MSG: ${{ github.event.head_commit.message }}
        PR_TITLE: ${{ github.event.pull_request.title }}
        ACTOR: ${{ github.actor }}
        REPO: ${{ github.repository }}
        RUN_URL: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
      run: |
        # 1. Echten Branch-Namen ermitteln (bei PRs ist HEAD_REF der Quell-Branch)
        if [ -n "$INPUT_BRANCH" ]; then
          BRANCH="$INPUT_BRANCH"
        elif [ -n "$HEAD_REF" ]; then
          BRANCH="$HEAD_REF"
        else
          BRANCH="$REF_NAME"
        fi

        # 2. Commit Message auflösen (Push-Event -> Head Commit, PR-Event -> PR Title, Fallback -> Git Log)
        if [ -n "$INPUT_COMMIT_MSG" ]; then
          COMMIT_MSG="$INPUT_COMMIT_MSG"
        elif [ -n "$EVENT_COMMIT_MSG" ]; then
          COMMIT_MSG="$EVENT_COMMIT_MSG"
        elif [ -n "$PR_TITLE" ]; then
          COMMIT_MSG="$PR_TITLE"
        else
          COMMIT_MSG=$(git log -1 --pretty=%B 2>/dev/null | head -n 1 || echo "No commit message")
        fi

        # 3. Status Styling festlegen
        if [ "$JOB_STATUS" = "success" ]; then
          COLOR="#2EB67D"
          STATUS_EMOJI="🟢"
        elif [ "$JOB_STATUS" = "cancelled" ]; then
          COLOR="#FFA500"
          STATUS_EMOJI="⚠️"
        else
          COLOR="#E01E5A"
          STATUS_EMOJI="🔴"
        fi

        # 4. Message escapen (JSON safe)
        SAFE_COMMIT_MSG=$(printf '%s' "$COMMIT_MSG" | jq -Rsa . | sed 's/^"//;s/"$//')

        PAYLOAD=$(cat <<EOF
        {
          "attachments": [
            {
              "title": "$STATUS_EMOJI $WORKFLOW_NAME $JOB_STATUS: $REPO",
              "title_link": "$RUN_URL",
              "color": "$COLOR",
              "text": "*Branch:* \`$BRANCH\`\n*Commit:* $SAFE_COMMIT_MSG\n*Actor:* $ACTOR"
            }
          ]
        }
        EOF
        )

        curl -s -X POST -H 'Content-Type: application/json' \
          --data "$PAYLOAD" \
          "$ROCKETCHAT_WEBHOOK_URL"
